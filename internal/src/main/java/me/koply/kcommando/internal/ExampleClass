package me.koply.kcommando.internal;

public class ExampleClass {

    @HandleCommand(alias, name, desc)
    boolean handle(event, args, prefix) {}

    @HandleCommand(alias, name, desc)
    void handle(event, args, prefix) {}

    @HandleSlash(name, desc, options={})
    void isimsiz(event) {}

    @HandleButton(name)
    void ismi(event) {}

    @SimilarCallback
    void zırnık(event, setofsimilarcommands)

    @HandleFalse
    void handleFalse(event, args, prefix)
}